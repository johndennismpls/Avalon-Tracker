<UserControl x:Class="AvalonTracker.QuestResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AvalonTracker" 
             mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <local:QuestCardView x:Name="Card1"/>
                <local:QuestCardView x:Name="Card2"/>
                <local:QuestCardView x:Name="Card3"/>
                <local:QuestCardView x:Name="Card4"/>
                <local:QuestCardView x:Name="Card5"/>
            </StackPanel>
            <Button Content="Submit Results!" Command="{Binding SubmitResultsCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
